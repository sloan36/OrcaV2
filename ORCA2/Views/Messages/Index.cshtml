@model IEnumerable<ORCA2.Models.Message>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Send New Message", "Create")
</p>
<table class="table">
    <tr>
        <!--<th>
            Html.DisplayNameFor(model => model.User1ID)
        </th>
        <th>
            Html.DisplayNameFor(model => model.User2ID)
        </th>-->
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InitialMessage)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <!--<td>
            Html.DisplayFor(modelItem => item.User1ID)
        </td>
        <td>
            Html.DisplayFor(modelItem => item.User2ID)
        </td>-->
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.InitialMessage)
        </td>
        <td>
            @Html.ActionLink("Reply", "Reply", new { id=item.MessageID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MessageID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MessageID })
        </td>
    </tr>
}

</table>
